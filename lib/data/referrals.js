// Community referral codes — populated by real user submissions via /api/community.
// Seed data intentionally empty until real codes are collected.
// Data shape: { id, type, name, code, desc, upvotes, date }
// Types: "Tesla" | "SunPower" | "Enphase" | "Rivian" | "Other"
export const REFERRALS = [];
